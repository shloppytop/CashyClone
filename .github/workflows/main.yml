name: Flutter iOS Deployment

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build and Deploy
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set Up Flutter
        uses: subosito/flutter-action@v1

      - name: Install Dependencies
        run: flutter pub get

      - name: Cache Dependencies
        uses: actions/cache@v4.0.2
        with:
          path: ~/.pub-cache
          key: flutter-pub-cache-${{ runner.os }}-${{ hashFiles('**/pubspec.lock') }}

      - name: Create IPA and .app
        run: |
          flutter build ios --release --no-codesign
          mkdir -p $HOME/outputs/ipa
          cp -R build/ios/iphoneos/Runner.app $HOME/outputs/ipa/
          cd $HOME/outputs/ipa
          zip -r app.ipa Runner.app


      - name: Publish Artifacts
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 587
          username: kthnxlol@gmail.com
          password: '@c1dr0cks'
          from: tootoo20490@gmail.com
          subject: "Build Artifacts"
          cc: nice
          body: "The build artifacts are attached."
          attachments: |
            $HOME/outputs/ipa/app.ipa
            build/ios/iphoneos/Runner.app
            /tmp/xcodebuild_logs/*.log
            flutter_drive.log

      - name: Cache Artifacts
        uses: actions/cache@v2
        with:
          path: |
            build/ios/iphoneos/Runner.app
            $HOME/outputs/ipa
            /tmp/xcodebuild_logs/*.log
            flutter_drive.log
            build/ios/iphoneos
